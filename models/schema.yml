version: 2
sources:
  - name: raw
    schema: gz_raw_data 
    tables:
      - name: sales
      - identifier: raw_gz_sales
      - name: product
      - identifier: raw_gz_product
      - name: ship
      - identifier: raw_gwz_ship
    

sources:
  - name: gz_raw_data
    tables:
      - name: users
        identifier: user_id
        columns:
          - name: user_id
            description: A unique identifier for users
          - name: email
            description: The email address of the user
      - name: orders
        identifier: order_id
        columns:
          - name: order_id
            description: A unique identifier for orders
          - name: user_id
            description: The user who placed the order